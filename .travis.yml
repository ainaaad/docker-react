sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t ainadomenech/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ainadomenech/docker-react npm run test
